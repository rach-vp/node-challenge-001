const authentication = require('./authentication');
const authorization = require('./authorization');
const authenticationAttempt = require('./autheticationAttempt');
const authorizationAttempt = require('./authorizationAttempt');

module.exports = {
  authentication,
  authorization,
  authenticationAttempt,
  authorizationAttempt,
};
